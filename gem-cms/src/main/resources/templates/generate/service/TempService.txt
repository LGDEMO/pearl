package ${package}.service;

import ${package}.model.po.${Entity};
import ${package}.model.vo.${Entity}Vo;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;

import java.util.List;

public interface ${Entity}Service {


    ${Entity} add(${Entity}Vo vo);

    List findListAll();

    List findListByParams(${Entity}Vo vo);

    Page findPageAll(Pageable pageable);

    Page findPageByParams(${Entity}Vo vo, Pageable pageable);

    ${Entity} update(${Entity}Vo vo);

    void delete(Long id);
}
